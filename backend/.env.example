# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=promotion_manager

# Or use a full DATABASE_URL (overrides individual settings)
# DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/promotion_manager

# Redis
REDIS_URL=redis://localhost:6379/0

# Security
SECRET_KEY=generate_a_strong_random_key_here
